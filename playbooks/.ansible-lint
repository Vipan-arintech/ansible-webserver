# .ansible-lint

# This is the configuration file for ansible-lint.
# Specify which rules to warn or skip here.

# Optional: Define the list of warning rules to skip
warn_list:  
  - '403'  # Package installs should not use latest
  - '301'  # Use the recommended package installation method

# Optional: You can also add other configurations, such as:
# skip_list:  # List of rules to silence completely
#   - '202'  # Skip rule 202 as an example

# You can customize your configuration further as needed.
